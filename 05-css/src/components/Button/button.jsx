import styles from "./button.module.css";

const Button = () => {
  return (
    <div className={styles.button}>
      <button>This is a button dude!</button>
    </div>
  );
};

export default Button;